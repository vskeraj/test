# Copy this file to .env.local and fill in real values. Never commit .env.local.

# MongoDB connection string (Atlas or local).
MONGODB_URI=mongodb://localhost:27017/firefly

# NextAuth — generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=replace_with_a_long_random_string
# Base URL of the app. Use the deployed URL in production (e.g. https://your-app.vercel.app).
NEXTAUTH_URL=http://localhost:3000

# Google OAuth credentials (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth credentials (https://developers.facebook.com/apps)
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
